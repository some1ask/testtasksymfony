{% extends 'base.html.twig' %}
{% block body %}
{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.href) }}
    {{ form_row(form.address) }}
    {{ form_row(form.phone) }}

    {{ form_row(form.save, { 'label': 'Update' }) }}

{{ form_end(form) }}

{% endblock %}